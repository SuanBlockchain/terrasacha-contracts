# ====================================
# LOCAL DEVELOPMENT ENVIRONMENT CONFIG
# ====================================
# This is an example configuration file for local development
# Copy this file to .env and update the values as needed
# Production secrets are stored in AWS Systems Manager Parameter Store

# Environment
ENVIRONMENT=development

# API Configuration
API_PORT=8000

# Cardano Network Configuration
network=testnet
blockfrost_api_key=your_blockfrost_testnet_api_key_here

# MongoDB Local Development (localhost)
# Install MongoDB locally: https://www.mongodb.com/docs/manual/installation/
MONGODB_ADMIN_URI=mongodb://localhost:27017/terrasacha_admin
MONGODB_URI_DEFAULT=mongodb://localhost:27017/terrasacha_default_development

# JWT Configuration (Local Development Only)
# Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=local-dev-secret-key-change-me
JWT_ALGORITHM=HS256
WALLET_SESSION_TIMEOUT_MINUTES=30
WALLET_REFRESH_TOKEN_DAYS=7

# Admin API Key (Local Development Only)
# Generate a secure key: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_API_KEY=local-dev-api-key-change-me

# ====================================
# PRODUCTION DEPLOYMENT
# ====================================
# Production secrets are managed in AWS Systems Manager Parameter Store
# Location: us-east-2 region
#
# To view production secrets:
#   ./scripts/manage-secrets.sh list
#
# To add/update a production secret:
#   copilot secret init
#
# Production secrets stored in Parameter Store:
#   - JWT_SECRET_KEY
#   - ADMIN_API_KEY (api_key_dev)
#   - MONGODB_ADMIN_URI
#   - MONGODB_URI_DEFAULT
#   - blockfrost_api_key
